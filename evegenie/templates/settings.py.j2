{% for endpoint, endpont_schema in endpoints.iteritems() %}
{{ endpoint }} = {{ endpont_schema }}
{% endfor %}


eve_settings = {
    'MONGO_HOST': 'localhost',
    'MONGO_DBNAME': 'testing',
    'RESOURCE_METHODS': ['GET', 'POST', 'DELETE'],
    'BANDWIDTH_SAVER': False,
    'DOMAIN': {
        {%- for k in endpoints %}
        '{{ k }}': {{ k }},
        {%- endfor %}
    },
}